# This file generates the GitHub Actions matrix
# For simple board + shield combinations, add them
# to the top level board and shield arrays, for more
# control, add individual board + shield combinations to
# the `include` property, e.g:
#
# board: [ "nice_nano_v2" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano_v2
#     shield: reviung41
#
---
include:
  - board: seeeduino_xiao_ble
    shield: lily58_dongle_xiao prospector_adapter
    cmake-args: -DCONFIG_BUILD_OUTPUT_UF2=y -DCONFIG_LV_COLOR_DEPTH=16
  - board: seeeduino_xiao_ble
    shield: settings_reset
    cmake-args: -DCONFIG_BUILD_OUTPUT_UF2=y
  - board: nice_nano_v2
    shield: lily58_left nice_view_adapter
    cmake-args: -DCONFIG_ZMK_SPLIT=y -DCONFIG_ZMK_SPLIT_ROLE_CENTRAL=n
  - board: nice_nano_v2
    shield: lily58_right nice_view_adapter
    cmake-args: -DCONFIG_ZMK_SPLIT=y -DCONFIG_ZMK_SPLIT_ROLE_CENTRAL=n
  - board: nice_nano_v2
    shield: settings_reset

